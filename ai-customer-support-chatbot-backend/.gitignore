node_modules/
# Keep environment variables out of version control
.env

/src/generated/prisma/

# ----------------------------------------------------------------------
# Firebase / Google Cloud service‑account secrets
# ----------------------------------------------------------------------
# Ignore only the admin SDK JSON files that start with “auth-”
src/config/firebase-service-account.json

# Keep other JSON config files that are needed
!src/config/firebase.js
!src/config/database.js
!src/config/prisma.js

# Keep npm / TypeScript config files
!package.json
!package-lock.json
!tsconfig.json